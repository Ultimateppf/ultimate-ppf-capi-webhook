[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn nuvemshop_whatsapp_capi:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
